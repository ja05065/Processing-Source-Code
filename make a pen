 ```
 void setup(){
  size(500,500);
  stroke(0,0,255);
  strokeWeight(16);
}
void draw(){
  if(mousePressed)
      line(pmouseX,pmouseY,mouseX,mouseY);  
}
```

펜을 만들어주는 코드이다.
가로 세로 500 사이즈에 파란색 펜이 나온다. 펜의 사이즈는 16이다.
